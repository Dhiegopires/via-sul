# Step 7.2 — Migração e Spec

Você está portando um componente de UI para o design-system VellumWire com base no inventário e nos tokens semânticos disponíveis levantados no passo anterior.

Regra: **CSS de saída usa exclusivamente `var(--semantic-*)`.** Verificação: `grep -E '#|rgb\(' <nome>.css` deve retornar vazio.

---

## INPUT

**Nome do componente:** [mesmo do 7.1]
**Categoria:** [mesmo do 7.1]

**HTML original:**
[cole aqui]

**CSS original:**
[cole aqui]

**JS original** (se houver):
[cole aqui — ou "nenhum"]

**Tabela de mapeamento do 7.1** (valores hardcoded → papel semântico):
[cole a tabela do passo anterior]

**Tokens semânticos disponíveis** (saída dos greps do 7.1 — só as categorias relevantes):
[cole aqui — não o arquivo inteiro, só a saída dos greps que você rodou]

**Variantes e slots identificados no 7.1:**
[cole aqui]

---

## O QUE ENTREGAR

### `<nome>.html`
Markup de referência. Classe wrapper específica (`.faq-accordion`, `.card-service`, etc.). Conteúdo substituído por placeholders descritivos: `[Pergunta]`, `[Resposta]`. Slots opcionais comentados.

### `<nome>.css`
Todos os valores substituídos pelos `--semantic-*` mapeados. Variações via `[data-variant]`, `[data-color]`, etc. — nunca classes novas por variação. `focus-visible` em todo elemento interativo. Animações com `var(--semantic-motion-*)`. Nenhum hardcode.

### `<nome>.js` (se aplicável)
ES6, vanilla, sem dependências, sem `console.log`.

### `<nome>.spec.md`

````markdown
# [Nome]

```json
{
  "component": "<nome>",
  "category": "<categoria>",
  "rootSelector": ".<classe-raiz>",
  "version": 1,
  "status": "stable",
  "variants": { "data-<attr>": ["valor1", "valor2"] },
  "slots": [{ "name": "<slot>", "selector": ".<classe>", "required": false }],
  "states": ["hover", "focus-visible", "active"],
  "tokensUsed": ["semantic.color.*", "semantic.size.*"]
}
```

## Origem
De qual site de cliente veio. Decisões não-óbvias de implementação.

## Quando usar
Uma linha.

## Markup canônico
Exemplo mínimo funcional.

## Como adaptar
O que muda via `data-*`. Quando ir para `docs/versioning-policy.md` em vez de editar aqui.
````

---

## GUARDRAILS

- Não invente tokens. Se um `--semantic-*` não está no input, não existe.
- Não invente estrutura. Extraia e abstraia o que está no input.
- Output completo. Sem `...` ou stubs.
